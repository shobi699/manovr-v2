# Dependencies
node_modules/

# Local development
.claude/
mockups/

# Playwright CLI artifacts
.playwright-cli/

# Mobile workflow config (may contain credentials)
workflows/mobile-config.json

# QA learnings ledger (per-project, not committed to plugin repo)
.qa-learnings/
