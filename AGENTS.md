<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version has breaking changes — APIs, conventions, and file structure may all differ from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing any code. Heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# قوانین پروژه Manovr V3

## اصول طراحی و رابط کاربری (RTL & Aesthetics)
1. **راست‌چین بودن (RTL)**: تمام المان‌ها باید از چینش RTL پیروی کنند (`dir="rtl"`). از کلاس‌های منطقی Tailwind (مثلاً `ms-*` و `me-*` به جای `ml-*` و `mr-*`) استفاده کنید.
2. **ظاهر مدرن و ممتاز (Premium)**: از رنگ‌های تخت و پیش‌فرض (مانند قرمز و آبی ساده) خودداری کنید. تم تاریک و روشن را با ترکیب رنگ‌های مدرن و شیشه‌ای (Glassmorphism) توسعه دهید.
3. **حالات تعاملی**: تمام دکمه‌ها و المان‌های تعاملی باید حالات hover، focus و active روان و مشخص به همراه انتقال‌های نرم (Transitions) داشته باشند.
4. **حجم و بهینه‌سازی صحنه سه‌بعدی**: برای صحنه سه‌بعدی از مدل‌های ساده (Low-poly) استفاده کنید و همواره نمای دوبعدی (2D vector layout) را به عنوان جایگزین در نظر بگیرید.

## امنیت و سطوح دسترسی
1. **بررسی دسترسی**: در تمامی Server Actionها به جای متدهای ساده‌ی `isAdmin` از تابع `hasPerm(session, permission)` استفاده کنید.
2. **امنیت توکن**: مجوزهای مؤثر کاربر را در JWT ذخیره کنید تا از کوئری‌های مکرر به دیتابیس جلوگیری شود.

## قانون همگام‌سازی سه‌گانه (Tri-Sync Protocol — نقش‌ها، منوها و آموزش) — الزام‌آور و قطعی
مطابق دستور مستقیم کارفرما، هر زمان که فایل جدید، منوی جدید، صفحه، مودال، مسیر یا قابلیت عملیاتی جدیدی در سیستم ایجاد یا ویرایش می‌شود، ایجنت موظف است این سه گام را همزمان و بدون استثنا اجرا کند:
1. **سیستم مجوزها (`src/lib/perms.ts`)**: تعریف کلید دسترسی جدید در `ALL_PERMS`، برچسب فارسی واضح در `PERM_LABELS`، تخصیص به گروه متناظر در `PERM_GROUPS` و اعمال گارد `hasPerm(session, permission)`.
2. **پنل مدیریت نقش‌ها (`/roles`)**: اطمینان از ظاهر شدن مجوز در گروه‌های موضوعی فرم ایجاد و ویرایش نقش جهت تخصیص اختیارات توسط مدیران پایانه.
3. **مرکز آموزش و دانشنامه (`/help` و `src/lib/help-menu-guides.ts`)**: مستندسازی و آموزش تفصیلی قابلیت جدید در منوی مربوطه با رعایت سقف الزامی **حداقل ۱۰۰۰ کاراکتر** متن فنی و تخصصی (بدون هیچ‌گونه متن ساختگی یا Placeholder).

## گزارش‌ساز و ایمپورت/اکسپورت اکسل
1. **گزارش‌ساز پویا**: در گزارش‌ساز ورودی‌ها را به طور ایمن فیلتر کنید. از فیلدها و شرط‌های معتبر اعتبارات‌سنجی شده استفاده کنید.
2. **راست‌چین کردن فایل‌های خروجی**: فایل‌های Excel خروجی از `exceljs` باید حتماً ویژگی راست‌چین (`views: [{ rtl: true }]`) داشته باشند.
3. **PDF فارسی**: در تولید PDF از کتابخانه‌ی `pdfmake` به همراه فونت فارسی وزیرمتن به صورت Embed شده استفاده کنید.

## زمان و تقویم رسمی (Tehran Timezone & Jalali Calendar)
1. **منطقه زمانی تهران**: تمامی تاریخ‌ها و زمان‌ها در پروژه باید به وقت تهران (`Asia/Tehran`) نمایش داده شوند.
2. **تقویم شمسی جلالی**: تمام تاریخ‌ها در فرانت‌اند، گزارش‌ساز، خروجی‌های PDF و Excel باید با فرمت تقویم شمسی (`fa-IR` با آپشن‌های `calendar: "persian"` و `timeZone: "Asia/Tehran"`) رندر شوند.

## ممنوعیت قطعی و ابدی صحبت از IP، پورت و وب‌سرور خارجی (شبکه ایزوله مترو)
1. **انسداد کامل پورت‌ها**: در شبکه حیاتی و ایزوله متروی تهران، به دلایل الزامات حراستی و پدافندی، کلیه پورت‌های مستقیم TCP/UDP بین سرور و کلاینت‌ها مسدود است.
2. **ممنوعیت تام IP/Port**: ایجنت هرگز و تحت هیچ شرایطی نباید راه‌حل‌های مبتنی بر IP، پورت، وب‌سرور مجزا یا مرورگر با IP پیشنهاد دهد.
3. **تنها بستر مجاز**: تنها بستر پایگاه داده مشترک، پوشه اشتراکی ویندوز (**SMB / DFS Share**) در مسیر `\\srvdfs01\Line1\Depo\data` است.
4. **تعبیه کامل در نرم‌افزار**: تمام ابزارهای تست شبکه، عیب‌یابی دسترسی و تعمیر دیتابیس باید بدون نیاز به فایل‌ها یا اسکریپت‌های جانبی، مستقیماً داخل خود نرم‌افزار (صفحه ورود و پنجره عیب‌یابی) تعبیه شوند.



## مطالعه الزامی اسکیل‌ها پیش از شروع عملیات (Pre-execution Skill Loading):
   - هر ایجنت باید بر اساس ماهیت تسک، اسکیل مرتبط در دایرکتوری `.agents/skills/` را بررسی کرده و استانداردها و الگوهای آن را مبنای کار قرار دهد.
 **چرخه تست تکرارشونده و سخت‌گیرانه پیش از خروج از تسک (Strict Self-Correction & Verification Loop):**
   - پس از اعمال هرگونه تغییر یا انجام هر تسک، ایجنت موظف است در یک چرخه تکرارشونده (حداقل ۲ بار متوالی)، صحت تغییرات را با ابزارهای تست خودکار، بررسی خطاها و سلامت ران‌تایم بسنجد.
   - ایجنت نباید تا پیش از اطمینان کامل از رفع عیب و برآورده شدن کیفیت عالی، تسک را تمام‌شده اعلام کند. هرگونه خطای سینتکس، اخطار کنسول یا رگرسیون باید بلافاصله درون همان چرخه برطرف گردد.
هنگام کار روی هر بخش از پروژه، ایجنت موظف است مطابق جدول زیر اسکیل‌های مربوطه را رعایت کند:

### الف) شناخت معماری، کاوش گراف و تحلیل وابستگی‌ها (Graphify Knowledge Engine)
* **`graphify-windows`** (`.agents/skills/graphify`):
  - کاوش و کوئری روی گراف ساختاری پروژه (`graphify query`, `graphify-out/graph.json`)
  - استخراج اتصالات ماژول‌های PHP به اسکریپت‌های JS و تحلیل وابستگی‌ها
  - به‌روزرسانی خودکار گراف در انتهای هر تسک توسعه

### ب) فریم‌ورک جامع تست و تضمین کیفیت (QA Skills & Playwright E2E Testing)
* **تولید سناریوهای کاربری (Generators)**:
  - `desktop-workflow-generator`: کشف روت‌ها و تدوین سناریوهای E2E دسکتاپ در `workflows/desktop-workflows.md`.

  - `multi-user-workflow-generator`: اعتبارسنجی فرآیندهای چندکاربره و سنکرون (تاییدیه مانور بین کاربران مختلف).
* **تبدیل به کد و اجرای تست (Converters & Runner)**:
  - `desktop-workflow-to-playwright`: کامپایل سناریوهای متنی به تست‌های Playwright در `e2e/desktop/`.
  - `playwright-runner`: اجرای تعاملی و مانیتورینگ آزمون‌ها.
* **ممیزی خودکار (Auditors & Agents)**:
  - استفاده از روبیک‌های ممیزی `smoke-tester`، `ux-auditor` (تطابق کامل با RTL و دیزاین سیستم)، `adversarial-breaker` (آزمون‌های مخرب و تزریق داده)، `security-auditor` (کنترل `hasPerm` و سشن‌ها) و `performance-profiler`.
  - خروجی ممیزی‌ها در پوشه `workflows/` یا `qa-reports/` مستند می‌شود.

### ج) مهارت‌های پیشرفته آزمون‌های E2E و دیباگ (Advanced E2E Skills — Suite 1.16.0)
هنگام نوشتن، بازبینی و دیباگ تست‌های Playwright و Cypress، ایجنت موظف به رعایت الگوهای استاندارد زیر است:

* **تولید هوشمند تست‌ها (`playwright-test-generator`)**:
  - تحلیل شکاف پوشش آزمون (Coverage-gap Analysis) پیش از کدنویسی.
  - کاوش در صفحات سیستم و پیشنهاد سناریوهای تست به همراه اخذ تاییدیه از کاربر.
  - تولید کدهای تست اتمیک، خوانا و با استانداردهای مدرن Playwright.

* **بازبینی و ممیزی کدهای تست (`e2e-reviewer`)**:
  - بررسی استاتیک تست‌ها در برابر ۲۴ الگوی ضدالگو (Test Smells) در سه سطح اولویت P0 (بحرانی)، P1 (متوسط) و P2 (نگهداری).
  - **کشف و ممنوعیت تست‌های «سبز کاذب» (False Greens)**:
    - ۱. هرگز از چک‌های شیئی روی Locator استفاده نکنید (مانند `expect(locator).toBeDefined()` یا `not.toBeNull()`)؛ همواره از Web-First Assertions مانند `await expect(locator).toBeVisible()` استفاده شود.
    - ۲. کلمه کلیدی `await` برای کلیه توابع `expect` در Playwright الزامی است (جلوگیری از Floating Promises).
    - ۳. عدم استفاده از `test.only` یا `it.only` در کدهای متعهد شده به مخزن.
    - ۴. اثبات امکان Fail شدن تست: تست باید در صورت خرابی واقعی کامپوننت، قطعاً قرمز شود.
    - ۵. اعتبارسنجی پایداری داده (Persistence Proof): در تست‌های مربوط به تغییر وضعیت قطار، لاین یا مانور، تست باید علاوه بر تغییرات بصری UI، ذخیره شدن داده در بک‌اند را نیز اثبات کند.

* **ریشه‌یابی و دیباگ خودکار شکست تست‌ها (`playwright-debugger` و `cypress-debugger`)**:
  - تحلیل فایل‌های خروجی و گزارش‌های `playwright-report/` یا `test-results/`.
  - طبقه‌بندی دقیق خطای رخ‌داده بر اساس کدهای استاندارد ۱۵ گانه (F1 تا F15):
    - `F1`: شکست در مکان‌یاب (Locator Not Found / Selector Drift)
    - `F2`: تایم‌اوت زمان انتظار (Action Timeout)
    - `F3`: خطای عدم تطابق Assertion (Assertion Mismatch)
    - `F4`: انقضا یا شکست در نشست و احراز هویت (Auth/Session Failure)
    - `F5`: مسدود شدن المان یا مشکل کلیک‌پذیری (Actionability / Element Intercepted)
    - `F6`: خطای شبکه یا API (Network / 500 / CORS Failure)
    - و سایر خطاهای مرتبط با کرش، ریس‌کاندیشن، هیدریشن و متغیرهای محیطی.
  - ارائه راه‌حل اصلاحی قطعی (Fix) همراه با مستندات خطای کنسول و استک تریس.

### د) چارچوب توسعه مبتنی بر مشخصات GitHub Spec Kit (Spec-Driven Development — SDD)
سیستم مانور به صورت رسمی به ابزار استانداردهای مهندسی **GitHub Spec Kit (`specify-cli`)** و یکپارچگی اختصاصی **Antigravity IDE (`agy`)** مجهز شده است. تمامی فیچرها، اصلاحات و تسک‌ها باید با این چرخه استاندارد هماهنگ باشند:

* **قانون اساسی سیستم (`.specify/memory/constitution.md`)**:
  - مرجع عالی قوانین پروژه مانور. هر پیشنهاد معماری و هر Plan موظف به اعتبارسنجی با گیت‌های قانون اساسی (Tri-Sync، RTL، تقویم جلالی، امنیت Zod، تنظیم‌پذیری مدیر و آزمون‌های Playwright) است.

* **چرخه فرمان‌های SDD در مهارت‌های ایجنت (`.agents/skills/`)**:
  - `/speckit-constitution`: بازبینی و هماهنگی با اصول حاکم بر سیستم مانور.
  - `/speckit-specify`: تدوین مشخصات ویژگی در `specs/<feature>/spec.md` شامل داستان‌های کاربری اولویت‌دار (P1, P2) و سناریوهای پذیرش (Given/When/Then).
  - `/speckit-clarify`: شفاف‌سازی ابهامات پیش از فاز طراحی.
  - `/speckit-plan`: تدوین برنامه فنی در `specs/<feature>/plan.md` و ارزیابی Constitution Gates.
  - `/speckit-checklist`: تولید چک‌لیست‌های کیفی الزامات.
  - `/speckit-tasks`: شکست وظایف به تسک‌های اتمیک مستقل و موازی در `specs/<feature>/tasks.md`.
  - `/speckit-analyze`: گزارش انطباق میان اسناد مشخصات، برنامه و تسک‌ها.
  - `/speckit-implement`: پیاده‌سازی کامل کدها مطابق تسک‌ها بدون جا انداختن یا خلاصه کردن.
  - `/speckit-converge`: اعتبارسنجی نهایی، اجرای تست‌ها و اعلام وضعیت «همگرا» (Converged).

* **گردش کار آسیب‌شناسی و رفع باگ (Bug Triage Workflow)**:
  - `/speckit-bug-assess`: ثبت گزارش و ریشه‌یابی باگ در `.specify/bugs/<slug>/`.
  - `/speckit-bug-fix`: اصلاح دقیق کد بدون تغییرات ناخواسته.
  - `/speckit-bug-test`: راستی‌آزمایی با آزمون‌های رگرسیون و ثبت گزارش تاییدیه رفع عیب.




<!-- SPECKIT START -->
For additional context about technologies to be used, project structure,
shell commands, and other important information, read the current plan
at specs/004-part-time-driver/plan.md
<!-- SPECKIT END -->
