# Desktop Workflows — Manovr V3 (سامانه یکپارچه مانور و ناوگان ریلی)

> Auto-generated by desktop-workflow-generator for Manovr V3.
> Last updated: 2026-09-11
> Application: Manovr Railway Shunting & Fleet Management System
> Base URL: http://localhost:3000

## Quick Reference

| # | Workflow | Priority | Auth | Steps |
|---|---------|----------|------|-------|
| 1 | احراز هویت و ورود به سامانه (Login Flow) | core | no | 5 |
| 2 | مدیریت کاربران و تخصیص نقش‌ها (Users & RBAC) | core | required | 6 |
| 3 | مانیتورینگ نمای دپو و خطوط ریلی (Depot 3D & 2D) | core | required | 5 |
| 4 | مدیریت قطارها و ناوگان ریلی (Trains Management) | feature | required | 5 |
| 5 | مدیریت خطوط و ایستگاه‌ها (Railway Lines) | feature | required | 5 |
| 6 | ثبت و تاییدیه عملیات مانور و کارت سیر (Manovr Approvals) | feature | required | 6 |
| 7 | بازرسی و لاگ امنیتی سیستم (Audit Log Trail) | edge | required | 4 |
| 8 | تولید گزارش‌های تحلیلی و خروجی اکسل/PDF (Reporting Engine) | feature | required | 5 |
| 9 | تعامل با آکاردئون سایدبار (Sidebar Accordion) | feature | required | 4 |
| 10 | منوی کلیک‌راست هوشمند در نمای پایانه (Context Menu Engine) | feature | required | 6 |

---


## Core Workflows

## Workflow 1: احراز هویت و ورود به سامانه (Login Flow)
<!-- auth: no -->
<!-- priority: core -->
<!-- estimated-steps: 5 -->

> ورود ایمن کاربر به سامانه مانور با نام کاربری و رمز عبور و هدایت خودکار به داشبورد عملیاتی.

**Preconditions:**
- سرور نرم‌افزار در دسترس باشد.
- کاربر معتبر (مثلاً ادمین) در پایگاه داده وجود داشته باشد.

**Steps:**
1. Navigate to /login — صفحه ورود به سامانه مانور
   - Verify the login form is visible — فرم ورود نمایان است
   - Verify the username input field is visible — فیلد نام کاربری موجود است
   - Verify the password input field is visible — فیلد رمز عبور موجود است
2. Type "admin" in the username field — وارد کردن شناسه کاربری
3. Type "admin123" in the password field — وارد کردن کلمه عبور
4. Click the "ورود به سامانه" button — ارسال فرم ورود
5. Verify the URL is /dashboard — کاربر با موفقیت به داشبورد منتقل می‌شود
   - Verify the dashboard heading displays "داشبورد عملیات" — عنوان داشبورد نمایش داده می‌شود

**Postconditions:**
- کوکی یا توکن نشست کاربر در مرورگر ذخیره شده است.
- منوی ناوبری اصلی سیستم برای کاربر فعال است.

---

## Workflow 2: مدیریت کاربران و تخصیص نقش‌ها (Users & RBAC)
<!-- auth: required -->
<!-- priority: core -->
<!-- estimated-steps: 6 -->

> ایجاد کاربر جدید با نقش معین و بررسی بازتاب آن در جدول کاربران سامانه.

**Preconditions:**
- کاربر با نقش ادمین لاگین کرده باشد و مجوز MANAGE_USERS داشته باشد.

**Steps:**
1. Navigate to /users — ورود به صفحه لیست پرسنل و کاربران
   - Verify the users table is visible — جدول لیست کاربران نمایش داده می‌شود
2. Click the "کاربر جدید" button — باز شدن فرم ثبت کاربر جدید
   - Verify the URL is /users/new — هدایت به صفحه ثبت کاربر
3. Type "مهران قاسمی" in the fullName field — درج نام و نام خانوادگی
4. Type "m_ghasemi" in the username field — درج نام کاربری
5. Select "مانورچی" from the role dropdown — انتخاب نقش سازمانی کاربر
6. Click the "ذخیره کاربر" button — ذخیره اطلاعات کاربر
   - Verify the success toast shows "کاربر با موفقیت ثبت شد" — پیام تایید ثبت
   - Verify the user "مهران قاسمی" appears in the users table — مشاهده کاربر در لیست

**Postconditions:**
- کاربر جدید در دیتابیس SQLite/Prisma با پسورد هش‌شده ذخیره گردیده است.

---

## Workflow 3: مانیتورینگ نمای دپو و خطوط ریلی (Depot 3D & 2D)
<!-- auth: required -->
<!-- priority: core -->
<!-- estimated-steps: 5 -->

> مشاهده پلان دو بعدی و صحنه سه‌بعدی ایستگاه و کنترل وضعیت خطوط و ناوگان.

**Preconditions:**
- کاربر وارد سامانه شده باشد و دسترسی VIEW_DEPOT داشته باشد.

**Steps:**
1. Navigate to /depot — ورود به صفحه دپو و مانیتورینگ ایستگاه
   - Verify the depot container is visible — کانتینر دپو لود شده باشد
2. Verify the 2D railway layout is rendered — پلان دو بعدی خطوط ریلی رسم شده است
3. Click the "نمای سه‌بعدی" toggle button — تغییر حالت نمایش به 3D
   - Verify the 3D canvas is visible — کانواس Three.js رندر شده است
4. Click on line "خط ۱ دپو" in the layout — انتخاب خط اول دپو
   - Verify the line details sidebar shows "خط ۱ دپو" — سایدبار مشخصات خط را نشان می‌دهد
5. Click the "نمای دو بعدی" toggle button — بازگشت به نمای برداری ۲ بعدی سریع

**Postconditions:**
- آخرین وضعیت خطوط و لوکوموتیوها روی نقشه بارگذاری و به روز شده است.

---

## Feature Workflows

## Workflow 4: مدیریت قطارها و ناوگان ریلی (Trains Management)
<!-- auth: required -->
<!-- priority: feature -->
<!-- estimated-steps: 5 -->

> ثبت لوکوموتیو و قطار جدید با مشخصات فنی و طول قطار.

**Preconditions:**
- کاربر با نقش سرپرست یا ادمین لاگین کرده باشد.

**Steps:**
1. Navigate to /trains — صفحه لیست ناوگان و قطارها
   - Verify the trains list heading is visible — لیست قطارها نمایش داده شود
2. Click the "ثبت قطار جدید" button — کلیک روی دکمه افزودن
3. Type "قطار مانوری ۲۰۴" in the trainName field — نام قطار
4. Type "350" in the length field — طول قطار به متر
5. Click the "ذخیره قطار" button — ارسال فرم ذخیره
   - Verify the success toast appears — نمایش پیغام موفقیت‌آمیز
   - Verify the train "قطار مانوری ۲۰۴" is visible in the list — تایید حضور قطار در جدول

**Postconditions:**
- رکورد قطار در سیستم ثبت شده و آماده تخصیص به خطوط مانور است.

---

## Workflow 5: مدیریت خطوط و ایستگاه‌ها (Railway Lines)
<!-- auth: required -->
<!-- priority: feature -->
<!-- estimated-steps: 5 -->

> تعریف خط جدید در ایستگاه با تعیین ظرفیت و طول خط.

**Preconditions:**
- کاربر دارای مجوز مدیریت زیرساخت خطوط باشد.

**Steps:**
1. Navigate to /lines — صفحه مدیریت خطوط ریلی
   - Verify the lines grid is visible — جدول خطوط نمایش داده شود
2. Click the "افزودن خط جدید" button — دکمه ثبت خط
3. Type "خط فرعی ۶" in the lineName field — نام خط
4. Type "500" in the capacity field — ظرفیت خط
5. Click the "ذخیره" button — ثبت خط در سیستم
   - Verify the line "خط فرعی ۶" appears in the lines table — اطمینان از ثبت خط

**Postconditions:**
- خط ریلی در دیتابیس ایجاد شده و در نمای دپو قابل آدرس‌دهی است.

---

## Workflow 6: ثبت و تاییدیه عملیات مانور و کارت سیر (Manovr Approvals)
<!-- auth: required -->
<!-- priority: feature -->
<!-- estimated-steps: 6 -->

> ثبت دستور مانور انتقال واگن‌ها، گردش کار تاییدات چندمرحله‌ای و صدور کارت سیر.

**Preconditions:**
- حداقل یک قطار و دو خط در سیستم ثبت شده باشند.

**Steps:**
1. Navigate to /manovrs — ورود به لیست دستورات مانور
2. Click the "دستور مانور جدید" button — ایجاد مانور
3. Select "خط ۱ دپو" as sourceLine — انتخاب خط مبدا
4. Select "خط فرعی ۶" as targetLine — انتخاب خط مقصد
5. Click the "ثبت و ارسال به تایید" button — ارسال دستور مانور
   - Verify the status displays "در انتظار تایید" — وضعیت انتظار تایید
6. Navigate to /manovrs/approvals — رفتن به کارتابل تاییدات
   - Verify the pending manovr is visible in the approvals list — رویت مانور در کارتابل

**Postconditions:**
- فرآیند چندکاربره مانور ثبت و رویدادهای تغییر وضعیت در Audit Log درج می‌شوند.

---

## Edge Case Workflows

## Workflow 7: بازرسی و لاگ امنیتی سیستم (Audit Log Trail)
<!-- auth: required -->
<!-- priority: edge -->
<!-- estimated-steps: 4 -->

> رصد تمامی تراکنش‌ها، تغییرات حساس داده‌ها و ورود/خروج کاربران در لاگ‌های امنیتی با تاریخ جلالی.

**Preconditions:**
- کاربر دارای مجوز ADMIN_AUDIT باشد.

**Steps:**
1. Navigate to /admin/audit — صفحه لاگ‌های امنیتی و سیستم
   - Verify the audit logs table is visible — جدول لاگ‌ها نمایش داده شود
2. Verify all timestamps are in Jalali format with Tehran timezone — زمان‌ها با فرمت تقویم شمسی و ساعت تهران باشند
3. Type "MANOVR_CREATE" in the filter action box — فیلتر کردن براساس ایجاد مانور
4. Verify the filtered list displays only matching audit events — صحت عملکرد فیلترینگ گزارشات

**Postconditions:**
- ردیابی کلیه لاگ‌های سیستمی بدون افشای مسیرهای حساس شبکه اعتبارسنجی شد.

---

## Workflow 8: تولید گزارش‌های تحلیلی و خروجی اکسل/PDF (Reporting Engine)
<!-- auth: required -->
<!-- priority: feature -->
<!-- estimated-steps: 5 -->

> استخراج گزارش دوره‌ای، فیلتر بازه تاریخی شمسی و تولید فایل راست‌چین شده اکسل و PDF فارسی.

**Preconditions:**
- ماژول‌های ExcelJS و PDFMake در بک‌اند بارگذاری شده باشند.

**Steps:**
1. Navigate to /reports — صفحه گزارش‌ساز پویا
   - Verify the report builder interface is visible — محیط گزارش‌ساز لود شود
2. Select "گزارش عملکرد مانورهای شیفت" from report type — انتخاب نوع گزارش
3. Click the "دریافت خروجی اکسل (Excel)" button — درخواست دانلود اکسل
   - Verify the export action triggers successfully — آغاز فرآیند تولید فایل
4. Click the "دریافت خروجی پی‌دی‌اف (PDF)" button — درخواست دانلود PDF
   - Verify the PDF generator includes embedded Vazirmatn font — عدم بهم‌ریختگی حروف فارسی
5. Verify no 500 error or uncaught exception occurs — پایداری کامل سرور

---

## Workflow 9: تعامل با آکاردئون سایدبار (Sidebar Collapsible Accordion)
<!-- auth: required -->
<!-- priority: feature -->
<!-- estimated-steps: 4 -->

> بررسی عملکرد جمع‌شوندگی و باز شدن خودکار زیرمنوهای بخش تحلیل و تنظیمات در منوی کناری سامانه.

**Preconditions:**
- کاربر لاگین کرده باشد و سایدبار قابل مشاهده باشد.

**Steps:**
1. Navigate to /dashboard — ورود به داشبورد و مشاهده سایدبار
2. Locate the "تحلیل و تنظیمات" accordion button — یافتن کلید کنترل آکاردئون
3. Click to toggle expand/collapse — کلیک جهت تغییر وضعیت تاشوندگی
   - Verify smooth height transition occurs — انیمیشن ارتفاع روان اعمال می‌شود
   - Verify chevron icon rotates — آیکون چوران تغییر زاویه می‌دهد
4. Navigate to /reports — هدایت به زیرمسیر گزارش‌ساز
   - Verify auto-expansion opens the accordion — آکاردئون به صورت خودکار باز شده و لینک گزارش‌ساز فعال می‌گردد

**Postconditions:**
- وضعیت انتخاب کاربر در localStorage پایدار مانده و ناوبری بدون شکستگی انجام می‌پذیرد.

---

## Workflow 10: منوی کلیک‌راست هوشمند در نمای پایانه (Context Menu Engine)
<!-- auth: required -->
<!-- priority: feature -->
<!-- estimated-steps: 5 -->

> باز شدن منوی شیشه‌ای هوشمند سازمانی با کلیک‌راست، ارائه ابزارهای اختصاصی ریل و ناوگان و میانبرهای سراسری.

**Preconditions:**
- کاربر در صفحه نمای پایانه (/depot) باشد.

**Steps:**
1. Navigate to /depot — ورود به نمای تعاملی پایانه دپو
2. Right-click on the terminal canvas or line slot — کلیک‌راست روی بوم پایانه یا خانه ریل
   - Verify native browser menu is prevented — منوی پیش‌فرض مرورگر متوقف می‌شود
   - Verify custom glassmorphic context menu appears — منوی شیشه‌ای هوشمند با موقعیت مناسب نمایش می‌یابد
3. Verify terminal-specific tools are present — ابزارهای پایانه (قفل ریل، ثبت مانور، شناسنامه قطار، تغییر نما) نمایش دارند
4. Verify global shortcuts are present — دسترسی سریع به داشبورد، دفتر تلفن و کپی نشانی در دسترس است
5. Click on an option or trigger via Enter/Space — کلیک روی گزینه یا تریگر کیبورد
   - Verify target action executes deterministically prior to teardown — اکشن عملیاتی پیش از تخریب منو بدون تاخیر اجرا می‌شود
   - Verify context menu closes cleanly — منوی راست‌کلیک پس از اجرا بسته می‌شود
6. Press "Escape" key or click outside — فشردن کلید Esc یا کلیک در خارج منو
   - Verify context menu closes immediately without interfering with canvas drag-and-drop — منو بسته شده و هیچ تداخلی با کشیدن و رها کردن قطارها ندارد

**Postconditions:**
- رویدادهای کلیک‌راست و اکشن‌های انتخابی با حفظ کامل دسترسی‌پذیری و استقلال رویدادی اجرا می‌گردند.
- عملیات درگ-اند-دراپ قطارها روی ریل‌ها بدون هیچ‌گونه رگرسیون یا فریز بصری کار می‌کند.

---

## Appendix: Application Map Summary

### Routes
| مسیر (Route) | نوع دسترسی | فایل منبع | اولویت تست |
|---|---|---|---|
| `/login` | عمومی | `src/app/login/page.tsx` | Core |
| `/dashboard` | نیازمند لاگین | `src/app/(main)/dashboard/page.tsx` | Core |
| `/depot` | نیازمند لاگین | `src/app/(main)/depot/page.tsx` | Core |
| `/users` | ادمین | `src/app/(main)/users/page.tsx` | Core |
| `/roles` | ادمین | `src/app/(main)/roles/page.tsx` | Feature |
| `/trains` | اپراتور/ادمین | `src/app/(main)/trains/page.tsx` | Feature |
| `/lines` | اپراتور/ادمین | `src/app/(main)/lines/page.tsx` | Feature |
| `/manovrs` | عملیات | `src/app/(main)/manovrs/page.tsx` | Feature |
| `/phonebook` | پرسنل/عملیات | `src/app/(main)/phonebook/page.tsx` | Feature |
| `/reports` | گزارش‌گیری | `src/app/(main)/reports/page.tsx` | Feature |
| `/tickets` | پشتیبانی | `src/app/(main)/tickets/page.tsx` | Feature |
| `/settings` | شخصی‌سازی | `src/app/(main)/settings/page.tsx` | Feature |
| `/help` | آموزش | `src/app/(main)/help/page.tsx` | Feature |
| `/profile` | عمومی کاربر | `src/app/(main)/profile/page.tsx` | Feature |
| `/admin/audit` | مدیر سیستم | `src/app/(main)/admin/audit/page.tsx` | Edge |
| `/admin/terminals` | مدیر سیستم | `src/app/(main)/admin/terminals/page.tsx` | Edge |
| `/admin/lookups` | مدیر سیستم | `src/app/(main)/admin/lookups/page.tsx` | Edge |
| `/admin/branding` | مدیر سیستم | `src/app/(main)/admin/branding/page.tsx` | Edge |
| `/admin/backup` | مدیر سیستم | `src/app/(main)/admin/backup/page.tsx` | Edge |

