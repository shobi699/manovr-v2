# راهنمای توسعه پروژه Manovr V3 (سامانه مدیریت مانور پایانه فتح‌آباد)

## دستورات ترمینال
- **اجرای سرور توسعه**: `npm run dev`
- **ساخت پروژه نهایی**: `npm run build`
- **اجرای بررسی کدهای وب**: `npm run lint`
- **همگام‌سازی دیتابیس با اسکیما**: `npx prisma db push`
- **سید کردن دیتابیس نسخه ۳**: `node prisma/seed-v3.mjs`

## ساختار و فایل‌های جدید نسخه ۳
- **نمای پایانه (R3F 3D & 2D)**: `src/app/(main)/depot/page.tsx` و `DepotScene.tsx`
- **گزارش‌ساز پویا**: `src/app/(main)/reports/page.tsx` و `ReportBuilderClient.tsx`
- **مدیریت نقش‌ها (پرمیژن‌ها)**: `src/app/(main)/roles/page.tsx` و `RolesFormClient.tsx`
- **دفترچه تلفن پرسنل**: `src/app/(main)/phonebook/page.tsx` و `PhonebookClient.tsx`
- **بخش خروجی‌های پویا**:
  - خروجی اکسل راست‌چین: `/api/export/excel` (`src/app/api/export/excel/route.ts`)
  - خروجی پی‌دی‌اف فارسی: `/api/export/pdf` (`src/app/api/export/pdf/route.ts`)

## قوانین کدنویسی و اصول توسعه (Manovr V3)
1. **راست‌چین و زبان فارسی (RTL-First)**: تمام عناصر گرافیکی، جداول، فیلترها و کلمات باید راست‌چین بوده و زبان فارسی داشته باشند.
2. **سیستم مجوزهای سفارشی (`hasPerm`)**: بررسی دسترسی‌ها به جای فیلد ساده `isAdmin` با استفاده از اکشن `hasPerm(session, "perm.key")` بر روی پرمیشن‌های تخصصی صورت می‌گیرد.
3. **عدم هاردکد کردن مقادیر ظاهری**: تمامی متغیرهای رنگی، کیفیت ۳بعدی، فرکانس رفرش و تراکم جداول به‌صورت زنده از `AppSetting` و تم‌پرووایدر اعمال می‌شوند.
4. **شبیه‌ساز فارسی در PDF**: برای خروجی‌های پی‌دی‌اف در `pdfmake` به دلیل نبود پشتیبانی محلی از راست‌چین فارسی، از متد کمکی `farsi(text)` مبتنی بر `arabic-persian-reshaper` برای اصلاح فواصل حروف و معکوس‌سازی کلمات استفاده کنید.
5. **منطقه زمانی تهران و تقویم شمسی (Tehran Timezone & Jalali)**: تمامی زمان‌ها و تاریخ‌ها در سراسر پروژه (نماها، جداول، گزارش‌ساز، خروجی‌های اکسل/پی‌دی‌اف) باید با منطقه زمانی `Asia/Tehran` و تقویم رسمی شمسی جلالی (`calendar: "persian"`) رندر شوند.
