---
description: "قالب استاندارد شکست تسک‌های اجرایی قابلیت در سیستم مانور"
---

# فهرست وظایف اجرایی: [نام قابلیت] (Tasks: [FEATURE NAME])

**ورودی**: اسناد طراحی از مسیر `/specs/[###-feature-name]/`  
**پیش‌نیازها**: `plan.md` (الزامی)، `spec.md` (الزامی برای سناریوهای کاربری)، `data-model.md`

## ساختار کد تسک‌ها: `[ID] [P?] [Story/Phase] شرح تسک`
- **[P]**: تسک‌های موازی (فایل‌های مجزا بدون وابستگی متقابل).
- **[Story]**: انتساب به داستان کاربری (مثلاً US1, US2) یا فاز زیرساختی.
- **مسیر فایل‌ها**: مسیر دقیق فایل‌ها باید در توضیحات هر تسک درج شود.

---

## فاز ۱: زیرساخت داده و اسکیما (Phase 1: Setup & Data Schema)
**هدف**: آماده‌سازی مدل‌های پایگاه‌داده و اسکیماهای اعتبارسنجی

- [ ] T001 [P] به‌روزرسانی اسکیما در `prisma/schema.prisma` و اجرای مایگریشن
- [ ] T002 [P] تعریف اسکیماهای اعتبارسنجی Zod در `src/lib/validations/[feature].schema.ts`
- [ ] T003 [P] تعریف تایپ‌های اشتراکی TypeScript در `src/types/[feature].ts`

---

## فاز ۲: پروتکل امنیت و مجوزها (Phase 2: Security & Permissions — Tri-Sync Part 1)
**هدف**: استقرار کنترل دسترسی‌ها پیش از پیاده‌سازی منطق بیزینس

- [ ] T004 ثبت کلید دسترسی در `ALL_PERMS` و نام‌گذاری فارسی در `PERM_LABELS` درون `src/lib/perms.ts`
- [ ] T005 تخصیص دسترسی به گروه مرتبط در `PERM_GROUPS` درون `src/lib/perms.ts`
- [ ] T006 [P] نوشتن تست‌های واحد مجوزها در `src/lib/__tests__/perms.test.ts`

---

## فاز ۳: داستان کاربری ۱ (Phase 3: User Story 1 — 🎯 MVP)
**هدف**: پیاده‌سازی جریان اصلی سناریوی حیاتی P1

### آزمون‌های داستان کاربری ۱ (Playwright E2E & Vitest) ⚠️
> *توجه: تست‌ها باید ابتدا نوشته شوند و در صورت عدم پیاده‌سازی، قطعاً Fail شوند (قانون ضد سبز کاذب)*
- [ ] T007 [P] [US1] تدوین تست E2E در `e2e/desktop/[feature].spec.ts` با Web-First Assertions
- [ ] T008 [P] [US1] افزودن بررسی پایداری داده در دیتابیس (Persistence Proof) در سناریوی تست

### پیاده‌سازی سرور و بک‌اند
- [ ] T009 [US1] پیاده‌سازی Server Action در `src/app/actions/[feature].ts` با گارد `hasPerm` و اعتبارسنجی Zod
- [ ] T010 [US1] اعمال منطق زمانی با منطقه `Asia/Tehran` و ثبت وقایع در سیستم Audit Log

### پیاده‌سازی رابط کاربری و فرانت‌اند
- [ ] T011 [US1] ساخت کامپوننت‌های فرانت‌اند در `src/app/(main)/[feature]/` با چیدمان راست‌چین (`dir="rtl"`)
- [ ] T012 [US1] استفاده از کلاس‌های منطقی (`ms-*`, `me-*`) و تم مدرن شیشه‌ای (Glassmorphic)
- [ ] T013 [US1] اتصال کامپوننت کلاینت به Server Action همراه با مدیریت حالات Loading، Error و Toast فارسی

**نقطه بازرسی**: در این مرحله داستان کاربری ۱ به طور کامل و مستقل کار کرده و تست‌های آن پاس می‌شوند.

---

## فاز ۴: داستان کاربری ۲ و سناریوهای تکمیلی (Phase 4: User Story 2 & Enhancements)
**هدف**: پیاده‌سازی امکانات جانبی و بهبودهای عملیاتی

- [ ] T014 [P] [US2] آزمون سناریوی P2 در `e2e/desktop/[feature].spec.ts`
- [ ] T015 [US2] تکمیل اکشن‌های سرور تکمیلی
- [ ] T016 [US2] پیاده‌سازی کامپوننت‌های تکمیلی و فیلترهای جدول

---

## فاز ۵: تکمیل پروتکل همگام‌سازی سه‌گانه و مستندات (Phase 5: Tri-Sync Completion & Help Guide)
**هدف**: اتمام گام‌های ۲ و ۳ پروتکل همگام‌سازی سه‌گانه بدون استثنا

- [ ] T017 اعتبارسنجی و نمایش دسترسی جدید در فرم‌های مدیریت نقش‌ها (`src/app/(main)/roles/`)
- [ ] T018 نگارش آموزش تفصیلی و کاربردی در `src/lib/help-menu-guides.ts` و صفحه `/help` با رعایت **سقف حداقل ۱۰۰۰ کاراکتر فارسی**
- [ ] T019 [P] اجرای آزمون ناوگانی و ناوبری راهنما در `src/lib/__tests__/help-navigation.test.ts`

---

## فاز ۶: همگرایی، آزمون‌های رگرسیون و تحویل (Phase 6: Convergence & QA)
**هدف**: اطمینان از سلامت ۱۰۰٪ سیستم و اجرای دستور همگرایی

- [ ] T020 اجرای کلیه تست‌های Vitest با دستور `npm run test:run`
- [ ] T021 اجرای آزمون‌های Playwright E2E با دستور `npm run test:e2e`
- [ ] T022 اجرای دستور `/speckit-converge` جهت اثبات تطابق کد با مشخصات
- [ ] T023 پاکسازی کدهای اضافه، رفع هشدارهای ESLint و اطمینان از خروجی بدون باگ
