# مشخصات فنی و نیازمندی‌های قابلیت: [نام قابلیت] (Feature Specification: [FEATURE NAME])

**شاخه ویژگی (Feature Branch)**: `[###-feature-name]`  
**تاریخ ایجاد**: [تاریخ شمسی جلالی / میلادی]  
**وضعیت**: پیش‌نویس (Draft)  
**ورودی کاربر**: "$ARGUMENTS"

---

## ۱. سناریوهای کاربری و آزمون‌های پذیرش (User Scenarios & Acceptance Criteria) *(الزامی)*

<!--
  نکته مهم: داستان‌های کاربری باید بر اساس اولویت ارزش عملیاتی مرتب شوند (P1 حیاتی، P2 با اهمیت بالا، P3 تکمیلی).
  هر داستان کاربری باید به تنهایی قابلیت آزمون مستقل (Independently Testable) داشته باشد.
-->

### داستان کاربری ۱ - [عنوان مختصر] (اولویت: P1 🎯 MVP)
**شرح سناریو**: [شرح گام‌به‌گام تعامل کاربر با سیستم به زبان فارسی روان]

**دلیل اولویت**: [توضیح ارزش عملیاتی و ضرورت برای عملکرد پایانه مترو]

**روش آزمون مستقل**: [چگونه این سناریو به صورت ایزوله با Playwright یا آزمون دستی قابل اعتبارسنجی است]

**سناریوهای پذیرش (Acceptance Scenarios)**:
1. **فرضا (Given)** [وضعیت اولیه سیستم، مثلا کاربر با نقش متصدی مانور لاگین است], **هنگامی که (When)** [عملیات، مثلا دکمه ثبت مانور کلیک شود], **آنگاه (Then)** [نتیجه مورد انتظار، مثلا رکورد در جدول ثبت و تاییدیه سبز نمایش یابد].
2. **فرضا (Given)** [وضعیت خطا], **هنگامی که (When)** [ورودی نامعتبر ارسال شود], **آنگاه (Then)** [پیام خطای فارسی مشخص نمایش یابد].

---

### داستان کاربری ۲ - [عنوان مختصر] (اولویت: P2)
**شرح سناریو**: [شرح تعامل کاربر]

**دلیل اولویت**: [توضیح ارزش]

**روش آزمون مستقل**: [روش آزمون ایزوله]

**سناریوهای پذیرش**:
1. **فرضا (Given)** [وضعیت اولیه], **هنگامی که (When)** [عملیات], **آنگاه (Then)** [نتیجه].

---

## ۲. تحلیل همگام‌سازی سه‌گانه (Tri-Sync Impact Assessment) *(الزامی)*

- [ ] **مجوزها (`src/lib/perms.ts`)**:
  - نام کلید مجوز جدید (در صورت نیاز): `[PERM_KEY]`
  - عنوان فارسی مجوز: `[عنوان فارسی]`
  - گروه دسترسی: `[گروه در PERM_GROUPS]`
- [ ] **پنل مدیریت نقش‌ها (`/roles`)**:
  - بررسی نمایش مجوز جدید در ماتریس اختیارات نقش‌ها
- [ ] **مرکز آموزش و دانشنامه (`/help` و `src/lib/help-menu-guides.ts`)**:
  - نگارش مستندات آموزشی تفصیلی در منوی مربوطه با رعایت سقف **حداقل ۱۰۰۰ کاراکتر فارسی**.

---

## ۳. نیازمندی‌های تابعی و ساختار داده (Functional Requirements & Data)

### نیازمندی‌های تابعی (Functional Requirements)
- **FR-001**: سیستم باید [قابلیت عملیاتی مشخص، مثلا امکان تغییر وضعیت خط].
- **FR-002**: سیستم باید داده‌های ورودی را با اسکیماهای Zod در سمت سرور اعتبارسنجی کند.
- **FR-003**: کلیه تاریخ‌ها و ساعت‌ها باید در منطقه زمانی `Asia/Tehran` و با فرمت جلالی محاسبه و ثبت شوند.
- **FR-004**: دسترسی کاربر باید در اکشن‌های سرور با تابع `hasPerm(session, permission)` کنترل گردد.

### نیازمندی‌های شخصی‌سازی مدیر (Admin Settings)
- **SET-001**: [پارامتر یا ثابتی که باید در پنل تنظیمات مدیر قابل ویرایش باشد، بدون هاردکد].

---

## ۴. استانداردهای رابط کاربری و بومی‌سازی (UI & RTL Standards)

- **جهت صفحه و المان‌ها**: راست‌چین کامل (`dir="rtl"`) با کلاس‌های منطقی (`ms-*`, `me-*`).
- **تایپوگرافی**: فونت وزیرمتن، اعداد فارسی و پیام‌های خطای ۱۰۰٪ فارسی.
- **تم و استایل**: سازگار با تم تیره و روشن، جلوه‌های Glassmorphism و فیدبک‌های حرکتی روان.

---

## ۵. معیارهای موفقیت و پایداری (Success Criteria)

- **SC-001**: [شاخص موفقیت عملیاتی، مثلا صدور دستور مانور در کمتر از ۳ ثانیه].
- **SC-002**: آزمون‌های Playwright E2E با متدهای Web-First با موفقیت پاس شوند و تغییر وضعیت در دیتابیس (Persistence Proof) اثبات گردد.
- **SC-003**: هیچ تداخل یا رگرسیونی در سایر ماژول‌های پایانه ایجاد نشود.
