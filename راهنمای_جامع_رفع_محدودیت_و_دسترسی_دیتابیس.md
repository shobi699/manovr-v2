# راهنمای جامع رفع محدودیت‌ها، تنظیم دسترسی و عیب‌یابی پایگاه داده تحت شبکه
## سامانه یکپارچه مدیریت پایانه و مانور ریلی فتح‌آباد (نسخه ۰.۱.۲)
**تاریخ تدوین:** ۲۵ شهریور ۱۴۰۵ (1405/06/25)  
**مرجع توسعه:** مدیریت عملیات خط یک و ریاست عملیات پایانه و مانور خط یک  
**توسعه‌دهنده:** سید شبیر موسوی  

---

### ۱. مقدمه و معماری ارتباط تحت شبکه سازمانی

در ساختار شبکه داخلی شرکت بهره‌برداری راه‌آهن شهری تهران و حومه (مترو)، به دلایل الزامات امنیتی حاکم بر زیرساخت‌های حیاتی حمل‌ونقل ریلی:
1. **پورت‌های TCP/UDP مستقیم بر روی سرورها مسدود است** و امکان اجرای پایگاه داده‌های کلاینت-سرور که نیازمند باز بودن پورت‌های اختصاصی (نظیر 1433 یا 5432) هستند وجود ندارد.
2. ارتباط کلیه سیستم‌های کلاینت ایستگاه‌ها و دفاتر دپوی فتح‌آباد از طریق پروتکل استاندارد اشتراک فایل ویندوز (**SMB / DFS Share**) بر روی پوشه زیر تعریف شده است:
   ```plaintext
   \\srvdfs01\Line1\Depo\data
   ```
3. پایگاه داده مرکزی سامانه در مسیر `\\srvdfs01\Line1\Depo\data\database\dev.db` قرار داشته و تمامی کلاینت‌های مستقر در پایانه به صورت هماهنگ و همزمان از این منبع داده واحد استفاده می‌نمایند.

---

### ۲. نیازمندی‌های مجوز پوشه در سرور (Permissions Checklist)

برای اینکه کلاینت‌ها بتوانند اطلاعات مانور، قطارها و وضعیت‌ها را بدون هیچ‌گونه پیام خطا ثبت کنند، سیستم‌عامل سرور باید دو سطح دسترسی را برای کاربران مجاز فراهم کند:

#### الف) دسترسی اشتراک شبکه (Share Permissions):
* در سرور، روی پوشه `data` کلیک راست کرده و وارد `Properties -> Sharing -> Advanced Sharing -> Permissions` شوید.
* دسترسی گروه کاربران پرسنل دپو (یا `Domain Users` / `Everyone`) را روی:
  - **Change** = تیک فعال (Allow)
  - **Read** = تیک فعال (Allow)
  قرار دهید.

#### ب) دسترسی امنیتی دیسک (NTFS Security Permissions):
* در زبانه `Properties -> Security -> Edit`:
* گروه کاربری پرسنل را انتخاب کرده و اختیارات زیر را فعال فرمایید:
  - **Modify** (تغییر)
  - **Read & Execute** (خواندن و اجرا)
  - **List folder contents** (مشاهده محتویات)
  - **Read** (خواندن)
  - **Write** (نوشتن)
> **نکته بسیار مهم:** نرم‌افزار برای مدیریت تراکنش‌های همزمان، علاوه بر فایل اصلی `dev.db`، نیازمند ایجاد و حذف فایل‌های کمکی تراکنش در همان پوشه است؛ بنابراین داشتن حق دسترسی **Write** و **Modify** کاملاً الزامی است.

---

### ۳. ریشه‌یابی خطای دیسک ۲۵۷۰ SQLite و نحوه برطرف شدن قطعی آن

#### علت رخ‌داد خطا در نسخه‌های پیشین:
در پایگاه داده SQLite، اگر حالت ژورنال روی `WAL` (Write-Ahead Logging) تنظیم شده باشد، سیستم‌عامل ویندوز در محیط‌های اشتراک فایل شبکه (SMB) به دلیل سازوکار Sharing Violation، اجازه حذف فایل موقت ژورنال را در انتهای تراکنش نمی‌دهد و خطای زیر را صادر می‌کند:
```plaintext
SqliteError { extended_code: 2570, message: "disk I/O error" }
```
این خطا مانع از ثبت مانور جدید شده و در برخی مواقع کاربر را به صفحه ورود هدایت می‌کرد.

#### تدابیر مهندسی اعمال‌شده در نسخه ۰.۱.۲:
1. **تغییر حالت پایدار ژورنال به TRUNCATE:**
   در حالت `TRUNCATE`، فایل ژورنال در هنگام خاتمه تراکنش به جای حذف فیزیکی (Delete)، صرفاً طول آن صفر (Truncate) می‌شود. این ویژگی در پروتکل ویندوز SMB بدون هیچ تداخلی انجام شده و خطای ۲۵۷۰ را به صورت ۱۰۰٪ ریشه‌کن می‌سازد.
2. **مدیریت نوبت‌دهی و صف خودکار تراکنش‌ها (`executeWithRetry`):**
   در صورت همزمانی شدید ثبت مانور توسط چند کاربر، سیستم تا ۶ مرتبه با تاخیر تصادفی هوشمند (Exponential Backoff) تراکنش را نوبت‌دهی کرده و بدون خطا ثبت می‌نماید.
3. **پایش تاخیر واقعی شبکه (Real Physical Ping):**
   اندازه‌گیری زمان پاسخ‌دهی دیتابیس هم‌اکنون مستقیماً روی فایل شبکه سنجیده می‌شود و وضعیت پینگ واقعی (میلی‌ثانیه) در نوار بالای نرم‌افزار نمایش می‌یابد.

---

### ۴. ابزارهای آماده همراه بسته نرم‌افزاری

در این بسته، ابزارهای تخصصی آماده جهت راحتی مدیران و کارشناسان پایانه قرار گرفته است:

| ابزار | فایل اجرایی | شرح کاربرد |
| :--- | :--- | :--- |
| **ابزار عیب‌یابی دسترسی** | `check-database-permissions.bat` | با دوبار کلیک، وضعیت اتصال شبکه، مجوز خواندن، مجوز نوشتن، قفل فایل و سلامت دیتابیس را بررسی و گزارش فارسی می‌دهد. |
| **ابزار آزادسازی و تعمیر** | `repair-server-db.bat` | قفل‌های قدیمی دیتابیس را پاکسازی کرده، فایل‌های موقت را ادغام و حالت ژورنال دیتابیس سرور را به `TRUNCATE` تبدیل می‌نماید. |
| **سازنده میانبر کلاینت** | `create-client-shortcut.bat` | میانبر مستقیم، فوق‌سریع و بهینه‌شده نرم‌افزار را روی دسکتاپ کلاینت ایجاد می‌کند. |

---

### ۵. دستورالعمل گام‌به‌گام استقرار و بهره‌برداری اولیه

۱. **استقرار فایل‌ها در سرور:**
   - پوشه اکسترکت‌شده `ManovrSystem` و فایل `manovr-config.json` را در مسیر سرور دپو کپی فرمایید.
   - مطمئن شوید فایل پایگاه داده اولیه در مسیر `\\srvdfs01\Line1\Depo\data\database\dev.db` مستقر شده است.

۲. **آزادسازی اولیه دیتابیس (فقط یک‌بار):**
   - از روی یک رایانه متصل به شبکه دپو، فایل `repair-server-db.bat` را اجرا کنید.
   - پس از مشاهده پیام سبز رنگ «عملیات با موفقیت ۱۰۰٪ به پایان رسید»، دیتابیس برای تمام کلاینت‌ها آماده است.

۳. **راه‌اندازی سیستم کلاینت‌ها:**
   - روی کامپیوتر پرسنل و مسئولین مانور، روی فایل `create-client-shortcut.bat` دوبار کلیک نمایید تا میانبر برنامه روی دسکتاپ ایجاد گردد.
   - با کلیک روی آیکون برنامه، نرم‌افزار ظرف ۱ ثانیه باز شده، نشانگر دیتابیس به رنگ سبز درآمده و آماده ثبت مانورها می‌باشد.

---

### ۶. نکات کلیدی پشتیبانی و رفع اشکال اضطراری

* **اگر چراغ سرور قرمز بود یا پیام قطعی داده شد:**
  ابزار `check-database-permissions.bat` را اجرا کنید تا فوراً علت (عدم اتصال کابل، تغییر مسیر سرور یا کمبود مجوز نوشتن) مشخص گردد.
* **تنظیمات آنتی‌ویروس:**
  در صورتی که آنتی‌ویروس‌های شرکتی مانع دسترسی سریع به فایل‌های موقت شبکه می‌شوند، مسیر `\\srvdfs01\Line1\Depo\data` را در لیست استثنائات (Exclusions) آنتی‌ویروس ویندوز قرار دهید.
* **پشتیبانی فنی:**
  جهت دریافت راهنمایی‌های بیشتر، از بخش «تیکت‌های پشتیبانی» داخل نرم‌افزار یا مرکز آموزش و دانشنامه (`/help`) استفاده فرمایید.
