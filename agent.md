# راهنمای الزامات، معماری و استانداردهای پروژه مانور (Agent Guidelines)

این سند مرجع رسمی و الزام‌آور برای تمامی ایجنت‌ها و توسعه‌دهندگان سیستم مدیریت پایانه و مانور خط یک متروی تهران (پایانه فتح‌آباد) است. تمامی مفاد مندرج در `AGENTS.md` و این فایل باید پیش از هرگونه توسعه یا ایجاد فایل مطالعه و اجرا شوند.

---

## ۱. قانون همگام‌سازی سه‌گانه (Tri-Sync Mandatory Protocol) — الزام‌آور و قطعی
مطابق دستور مستقیم و مؤکد کارفرما، هر زمان که هرگونه فایل جدید، منوی جدید، صفحه، مودال، مسیر روت، یا قابلیت عملیاتی در سیستم اضافه یا ویرایش شود، ایجنت موظف است این سه گام را به صورت همزمان و بدون استثنا انجام دهد:

1. **سیستم مجوزها (`src/lib/perms.ts`)**:
   - تعریف کلید شناسه مجوز در آرایه `ALL_PERMS` (مانند `manovr.report` یا `report.schedule`).
   - تعریف برچسب و عنوان فارسی توصیفی و دقیق در `PERM_LABELS`.
   - تخصیص مجوز به گروه موضوعی مرتبط در `PERM_GROUPS` (شامل آیکون، عنوان و توضیحات فارسی).
   - پیاده‌سازی گارد امنیتی سمت سرور در Server Actionها یا صفحات با تابع `hasPerm(session, permission)`.

2. **پنل مدیریت نقش‌ها و سطوح دسترسی (`/roles`)**:
   - تضمین نمایش دسترسی جدید در گروه‌بندی فرم‌های ایجاد و ویرایش نقش جهت امکان فعال‌سازی یا لغو توسط مدیر.
   - اعمال دسترسی در نقش‌های سیستمی مانند «ادمین» (`isSystem: true`).

3. **مرکز آموزش و دانشنامه سامانه (`/help` و `src/lib/help-menu-guides.ts`)**:
   - مستندسازی کامل سناریوی کاربری، نکات ایمنی و گردش‌کار قابلیت جدید در راهنمای منوی مربوطه.
   - رعایت شرط الزامی **حداقل ۱۰۰۰ کاراکتر** متن فنی، تخصصی و مستند برای هر منو بدون هرگونه متن تکراری یا ساختگی (Zero Placeholder).

---

## ۲. اصول طراحی و رابط کاربری (RTL & Aesthetics)
1. **راست‌چین بودن کامل (RTL-First)**: کلیه المان‌ها باید ویژگی `dir="rtl"` داشته باشند. استفاده از کلاس‌های منطقی استاندارد (`ms-*` و `me-*` به جای `ml-*` و `mr-*`) الزامی است.
2. **طراحی ممتاز و شیشه‌ای (Premium Glassmorphism)**: پرهیز از رنگ‌های خام و دیفالت؛ استفاده از توکن‌های طراحی `var(--panel)`، `var(--line)` و `var(--accent)` با ترنزیشن‌های نرم و روان.
3. **بهینگی صحنه سه‌بعدی و دوبعدی**: سبک بودن مدل‌ها و ارائه همزمان نماهای دوبعدی، بنتو و نقشه پایانه به عنوان جایگزین سریع.

---

## ۳. امنیت، سطوح دسترسی و پایگاه داده
1. **امنیت دسترسی**: بررسی مداوم با `hasPerm(session, permission)` در تمامی Server Actionها.
2. **سلسله‌مراتب نقش‌ها**: کنترل لول کاربری با `isRoleAllowedToManage` جهت جلوگیری از تغییر نقش‌های بالادستی توسط نقش‌های پایین‌تر.
3. **تراکنش‌های اتمیک**: انجام کلیه جابجایی‌های قطار و مانورها درون تراکنش‌های `prisma.$transaction`.

---

## ۴. چرخه تست تکرارشونده و سخت‌گیرانه (Strict Self-Correction Loop)
- پس از اعمال هر تغییر، اجرای تست‌ها در حداقل ۲ دور متوالی الزامی است:
  1. بررسی استاتیک کامپایل با `npx tsc --noEmit`.
  2. اجرای تمامی آزمون‌های خودکار با `npm test` و تایید قبولی ۱۰۰٪ تست‌ها.
  3. بازبینی در مرورگر با ابزارهای Chrome DevTools جهت اطمینان از صفر بودن خطاهای کنسول و سلامت رندر.
